import Particles from "react-particles-js";
import { partilcesConfig } from "./particlesjs-config";

const ParticlesBackground = () => {
    return <Particles params={partilcesConfig}></Particles>;
};

export default ParticlesBackground;
